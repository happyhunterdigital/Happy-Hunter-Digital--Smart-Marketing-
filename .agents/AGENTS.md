# Project-Scoped Rules

- **Direct Deployment**: Always commit and push changes directly to the `main` branch (or configured default branch) of the GitHub repository. Do NOT create Pull Requests or branches for review unless explicitly requested by the user.
